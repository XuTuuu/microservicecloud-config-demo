spring:
  profiles:
    active:
    - dev
---
spring:
  profiles:
    active: dev
  application:
    name: microservicecloud-config-atguigu-dev
---
spring:
  profiles:
    active: test
  application:
    name: microservicecloud-config-atguigu-test

# 以UTF-8的格式保存！！！记事本打开